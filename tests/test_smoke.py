# Smoke test — ensures the test suite can be collected by pytest.
# Add real integration/unit tests here as the project grows.

def test_placeholder():
    """Placeholder so pytest exits 0 when no real tests exist yet."""
    assert True
