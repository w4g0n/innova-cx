-- Database initialization script
-- The database 'complaints_db' is already created by POSTGRES_DB environment variable

-- Just create a simple test to verify connection works
SELECT 'Database initialized successfully' AS status;